server:
  port: 8080

spring:
   profiles:
      active: development
   datasource:
     driverClassName: com.mysql.jdbc.Driver
     url: jdbc:mysql://localhost:3306/roadmapgenerator
     username: root
     password: JoyTua@02122018
   jpa:
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      show-sql: true
   servlet:
      multipart:
         enabled: true

logging:
  level:
    org:
       springframework: INFO